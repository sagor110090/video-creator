edge-tts==7.2.7
aiohttp==3.13.2
tabulate==0.9.0
certifi==2025.11.12
# FFmpeg is required but is a system dependency, not a python package.
# These are the core packages needed for the AI worker to function.
